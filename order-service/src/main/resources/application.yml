spring:
  application:
    name: order-service

  config:
    import: optional:configserver:http://localhost:8888

  # config:
  #   import: "optional:configserver:"
  # cloud:
  #   config:
  #     uri:
  #     - http://localhost:8888

  # profiles:
  #   active: local
